[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan storage:link --force && chmod -R 775 storage bootstrap/cache && php artisan config:cache && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
